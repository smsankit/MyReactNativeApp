import { View, Text } from 'react-native';
import React from 'react';

const CartScreen = () => {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text style={{ fontSize: 24, fontWeight: 'bold' }}>CartScreen</Text>
    </View>
  );
};

export default CartScreen;
